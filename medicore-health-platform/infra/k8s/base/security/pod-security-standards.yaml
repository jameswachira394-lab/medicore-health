# Enforces the "restricted" Pod Security Standard on the production
# namespace — no privilege escalation, no root, no host access.
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
