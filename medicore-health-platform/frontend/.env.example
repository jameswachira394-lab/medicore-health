# Base URL of the API Gateway. In docker-compose, points at the gateway's
# host-mapped port. In Kubernetes, this is baked in at build time via the
# VITE_API_BASE_URL build-arg (typically the Ingress path /api or the
# gateway's own subdomain).
VITE_API_BASE_URL=http://localhost:8000
